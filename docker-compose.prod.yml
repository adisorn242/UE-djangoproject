services:
  web:
    build: .
    container_name: firstweb
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env.prod
    volumes:
      - ./db.sqlite3:/app/db.sqlite3
      - ./media:/app/media
